#ifndef MAIN_H
#define MAIN_H

/**
 * File: main.h
 * 
 * Desc: Header file containing prototypes for all functions used in the 0x08-recursion directory.
 */

void _puts_recursion(char *s);
int _putchar(char c);
void _print_rev_recursion(char *s);
int _strlen_recursion(char *s);

#endif